# Requirements for Robotic Face Recognition and Emotion Analysis System
# Adjusted to avoid version conflicts between TensorFlow and PyTorch-related packages.
# This set targets a TensorFlow backend (DeepFace) which is the default used here.

# Web
Flask==3.1.2
flask-cors==6.0.1
gunicorn==23.0.0

# Computer Vision / ML (TensorFlow backend)
deepface==0.0.93
mediapipe==0.10.21
opencv-contrib-python==4.11.0.86

# Deep learning backend
tensorflow==2.12.0

# Core utilities
numpy==1.23.5
pillow==10.2.0
requests==2.32.5

# Optional / helper packages
python-dotenv==1.1.1
scipy==1.15.3
pandas==2.3.3

# Notes:
# - We pin numpy to 1.23.5 to maintain compatibility with TensorFlow 2.12.0.
# - If you prefer a PyTorch-only setup (facenet-pytorch / mtcnn), remove TensorFlow and use compatible
#   torch/facenet-pytorch versions instead. Those require numpy>=1.24 which conflicts with TF 2.12.
# - Installing these packages can be heavy (TensorFlow, mediapipe). Use a virtual environment.
